let fact = \(n: int): int => {
  if (n == 0) {
    return 1;
  } else {
    return n * fact(n - 1);
  }
}

let foo = \(n: int): int => {
  let bar = \() => \() => {
    print(10);
  }
  bar()();
  return 10;
}

export fact;
export foo;