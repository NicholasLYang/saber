let bar = \() => {
  let name = "Nicholas";
  let age = 10;
  \() => {
    printString("Nicholas");
    printInt(age);
  }
}

struct Person {
  name: string,
  age: int
}

let foo = \() => {
  let p = Person {
    name: "Nicholas",
    age: 21
  };
  let { name, age } = p;
  printString(name);
  printInt(age);
}

export foo;