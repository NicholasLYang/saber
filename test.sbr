foo bar: int (foo, bar) { foo, bar, baz }: A
let fact = \(n: int): int => {
  if (n == 0) {
    return 1;
  } else {
    return n * fact(n - 1);
  }
}

let foo = \(n: int): int => {
  let bar = \(m: int): int => {
    return m + 1;
  }
  return bar(n);
}

export fact;
export foo;